name: Mobile CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - 'mobile/**'
      - '.github/workflows/mobile-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'mobile/**'
      - '.github/workflows/mobile-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json
      
      - name: Install dependencies
        working-directory: mobile
        run: npm ci
      
      - name: Run ESLint
        working-directory: mobile
        run: npm run lint
      
      - name: Run TypeScript type checking
        working-directory: mobile
        run: npm run type-check
      
      - name: Run tests with Jest
        working-directory: mobile
        run: npm test -- --coverage --watchAll=false
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: mobile/coverage/coverage-final.json
          flags: mobile
          name: mobile-coverage
        continue-on-error: true

  build-android:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json
      
      - name: Install dependencies
        working-directory: mobile
        run: npm ci
      
      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      
      - name: Build Android APK with EAS
        working-directory: mobile
        run: |
          echo "EAS build would happen here"
          echo "Configure EXPO_TOKEN secret in repository settings"
          echo "Install EAS CLI and run: eas build --platform android --profile preview"
        # Uncomment when Expo token is configured:
        # run: |
        #   npm install -g eas-cli
        #   eas build --platform android --profile preview --non-interactive
      
      - name: Upload APK artifact
        if: success()
        run: |
          echo "APK artifact would be uploaded here"
        # uses: actions/upload-artifact@v4
        # with:
        #   name: android-apk
        #   path: mobile/build/*.apk
